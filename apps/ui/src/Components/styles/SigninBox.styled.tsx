import { Box, styled } from '@mui/material';

const SigininBox = styled(Box)`
    display: 'flex',
    flex-direction: 'column',
    align-items: 'center',
    min-height: '100vh',
    justifyContent: 'center',
    padding: '0 20px',
`;

export { SigininBox };
