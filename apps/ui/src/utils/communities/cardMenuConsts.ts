import {
  MANAGER_OPTIONS,
  MEMBER_OPTIONS,
} from '../../types/community-actions-enum';

const managerOptions: string[] = [MANAGER_OPTIONS.EDIT];
const userOptions: string[] = [MEMBER_OPTIONS.SEE_RIDES];

const ITEM_HEIGHT = 48;

export { managerOptions, userOptions, ITEM_HEIGHT };
