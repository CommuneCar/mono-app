import { Box } from '@mui/material';
import { styled } from '@mui/material';

const ExpandMoreFieldBox = styled(Box)(() => ({
  display: 'flex',
  flexDirection: 'column',
  justifyContent: 'left',
  alignItems: 'flex-start',
  marginBottom: '0.5rem',
}));

export { ExpandMoreFieldBox };
