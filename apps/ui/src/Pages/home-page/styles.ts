import { styled } from '@mui/material';

const Page = styled('div')(() => ({
  height: '100vh',
  width: '100vw',
  position: 'absolute',
  left: 0,
  top: 0,
  float: 'left',
  margin: 0,
}));

export { Page };
