import { Box, styled } from '@mui/material';

const Page = styled(Box)(() => ({
  top: 0,
  left: 0,
  margin: 0,
  float: 'left',
  width: '100vw',
  height: '100vh',
  position: 'absolute',
}));

export { Page };
