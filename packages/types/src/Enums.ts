export enum UserStatus {
  ACTIVE = 'Active',
  PENDING = 'Pending',
  REJECTED = 'Rejected',
  MANAGER = 'Manager',
}

export enum Gander {
  FEMALE = 'Female',
  MALE = 'Male',
  OTHER = 'Other',
}
