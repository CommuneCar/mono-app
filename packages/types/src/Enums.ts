export enum UserStatus {
  APPROVED = 'Approved',
  PENDING = 'Pending',
  REJECTED = 'Rejected',
  MANAGER = 'Manager',
}

export enum Gander {
  FEMALE = 'Female',
  MALE = 'Male',
  OTHER = 'Other',
}
