export enum UserStatus {
  APPROVED = 'Approved',
  PENDING = 'Pending',
  REJECTED = 'Rejected',
  MANAGER = 'Manager',
}

export enum Gender {
  FEMALE = 'Female',
  MALE = 'Male',
  OTHER = 'Other',
}
