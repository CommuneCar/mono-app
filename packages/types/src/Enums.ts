export enum UserStatus {
  ACTIVE = 'Active',
  PENDING = 'Pending',
  REJECTED = 'Rejected',
  MANAGER = 'Manager',
}

export enum UserRideStatus {
  PENDING = 'Pending',
  CANCELLED = 'Cancelled',
  CONFIRMED = 'Confirmed',
  REJECTED = 'Rejected',
}
export enum Gender {
  FEMALE = 'Female',
  MALE = 'Male',
  OTHER = 'Other',
}
